Hi, These folders correspond to the sequential steps to become a contact list in Python.

version1 = data stored in RAM
version2 = data stored in a JSON file
version3 = data stored in a database with SQLite
version5 = UI implemented with Tkinter with SQLite , one table in db. 
